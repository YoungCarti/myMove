import 'package:flutter/material.dart';

class SearchParkingScreen extends StatelessWidget {
  const SearchParkingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Search Parking')),
      body: const Center(
        child: Text('Search Parking Screen - To be implemented'),
      ),
    );
  }
}